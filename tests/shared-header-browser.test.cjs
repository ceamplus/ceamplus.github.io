const {chromium}=require('C:/Users/brigg/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict');const fs=require('node:fs');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});try{const p=await b.newPage();await p.route('https://fonts.googleapis.com/**',r=>r.fulfill({body:'',contentType:'text/css'}));const base=process.env.CEAM_TEST_URL||'http://127.0.0.1:8765';const files=fs.readdirSync('.').filter(n=>n.endsWith('.html'));const expected=['Home','Dashboard','Surveys','Assessment Center','Marketing Analytics Lab','Research','AI','Consulting','About'];for(const name of files){await p.goto(base+'/'+name);if(['mission.html','philosophy.html'].includes(name))await p.waitForURL('**/values.html*');assert.deepEqual(await p.locator('header .nav-links a').allTextContents(),expected,name);for(const width of [320,456,900,1440]){await p.setViewportSize({width,height:900});const m=await p.locator('.site-header').evaluate(h=>{const n=h.querySelector('nav');return {header:h.getBoundingClientRect().right,nav:n.scrollWidth,client:n.clientWidth,columns:getComputedStyle(n).gridTemplateColumns.split(' ').length,links:[...n.querySelectorAll('a')].map(a=>({left:a.getBoundingClientRect().left,right:a.getBoundingClientRect().right}))}});assert(m.header<=width+1,name);assert(m.nav<=m.client+1,name);assert.equal(m.columns,3,name);assert(m.links.every(a=>a.left>=0&&a.right<=width+1),name);}console.log('PASS shared header '+name);}await p.setViewportSize({width:900,height:600});await p.goto(base+'/assessment-center.html');await p.screenshot({path:'.cache/unified-header.png'});}finally{await b.close();}})().catch(e=>{console.error(e);process.exit(1)});
